#include <stdio.h>

int main()
{
    int X[10];
    int a = 0;
     for(int i=a;i<10;i++) {
        scanf("%d", &a);
        if(a<=0) {
            X[i] = 1;
        }else{
            X[i] = a;
        }
    }
    for(int i=0;i<10;i++) {
        printf("X[%d] = %d\n", i, X[i]);
    }
}
